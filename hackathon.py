n= 9
sock_list=[10,20,20,10]

def sock_merchant(n,socks_list):
    socks_list =set(sock_list)
    count=0
    for sock in sock_set:
        count += sock_list.count{sock} // 2
return count


sock_merchants(n,socks_lists)